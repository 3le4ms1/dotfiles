;;; scheme.el --- Configurazione per il linguaggio `Scheme'

;;; Commentary:
;; configurazione generale per il linguaggio `Scheme'

;;; Code:

(setq scheme-program-name "guile")

;;; scheme.el ends here.
