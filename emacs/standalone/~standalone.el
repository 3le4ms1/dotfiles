;;; ~standalone.el --- Configurazione dei vari file in `standalone'

;;; Commentary:
;; Questo è un file di configurazione che verrà eseguito dopo aver caricato i
;; vari file standalone

;;; Code:

(add-to-list 'auto-mode-alist '("\\.c\\'" . simpc-mode))

;;; ~standalone.el ends here.
