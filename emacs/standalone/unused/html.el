;;; html.el --- Configurazione `html-mode'

;;; Commentary:
;; Configurazione semplice per `html-mode'

;;; Code:

(setq sgml-quick-keys 'close)
;; (add-hook 'html-mode-hook #'sgml-mode)
;; (add-hook 'html-mode-hook #'emmet-mode)

;;; html.el ends here.
